# AI Agent 小说编写指南

## 项目信息
- 小说名：<小说名>
- 类型：<类型>
- 创建日期：<创建日期>

## 工作原则
1. 优先读取 `presets/outline.md`、`presets/characters.md` 和 `presets/world.md`等设定下面的文件，了解设定。
2. 写作前查看 `progress.md` 了解当前进度。
3. 新章节正文写入 `chapters/xxx_[章节名].md`，文件名保持顺序数字序号 + 章节名。
4. 修改设定前先在 `presets/` 对应文件中更新，再同步到正文。
5. 完成章节后更新 `progress.md` 的小说进度（章节、人物、大纲完成度等等）。

## 输出要求
- 使用 Markdown 格式。
- 章节内可适当分段，保持中文网络小说常见排版。
- 人物称呼、地名、功法/技能名须与 `presets/` 文件保持一致。
