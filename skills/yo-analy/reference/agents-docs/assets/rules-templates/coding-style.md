# 代码风格规则

> **使用说明**：只保留与该项目相关的约定，删除通用常识。按仓库实际类型（前端/后端）删除不适用章节。

---

## 1. 命名规范

### 文件命名

| 类型 | 规范 | 示例 |
|------|------|------|
| [组件/页面/类] | [PascalCase / kebab-case / snake_case] | [示例] |
| [工具函数/模块] | [camelCase / snake_case] | [示例] |
| [常量配置] | [UPPER_SNAKE_CASE] | [示例] |
| [测试文件] | [命名模式] | [示例] |

### 变量与函数命名

- [项目特有约定，如：布尔变量用 `is`/`has`/`should` 前缀]
- [项目特有约定，如：事件处理函数用 `handle` 前缀]
- [项目特有约定，如：异步函数用 `fetch`/`load`/`get` 区分语义]

---

## 2. 缩进与格式

- 缩进：[空格/制表符] x [数量]
- 行尾分号：[必须/可选/禁止]
- 引号：[单引号/双引号/反引号优先]
- 最大行宽：[数量]
- 格式化工具：[ESLint / Prettier / black / etc] 配置位置：[文件路径]

---

## 3. 注释规范

### 必须注释的场景

- [公共函数/复杂逻辑/业务规则等]

### 注释风格

- [JSDoc / docstring / 行内注释等]
- 示例：

```[语言]
/**
 * [函数说明]
 * @param [参数] - [说明]
 * @returns [返回值说明]
 */
```

---

## 4. Git 规范

### 分支命名

- [格式，如：feature/xxx, fix/xxx, hotfix/xxx]

### Commit 格式

- [Conventional Commits / 自定义格式]
- 示例：

```
[类型]([范围]): [描述]

[正文]
```

### PR 规范

- [要求、模板、Review 流程]

---

## 5. 项目特有约定

> 从代码库中提炼，不写通用常识。

- [约定 1：如特定目录下的文件必须使用某种模式]
- [约定 2：如某种业务场景必须使用特定工具函数]
- [约定 3：如组件 Props 的必填/可选规则]

### 正例 / 反例

```[语言]
// 正确
[正确示例代码]

// 错误
[错误示例代码]
```
