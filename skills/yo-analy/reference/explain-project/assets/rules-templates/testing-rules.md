# 测试规则

> **使用说明**：只保留与该项目相关的测试约定，删除通用常识。按仓库实际类型（前端/后端）删除不适用章节。

如果项目没有测试，则不生成此规则

---

## 1. 测试策略

| 测试类型 | 工具 | 目录 | 覆盖范围 |
|----------|------|------|----------|
| 单元测试 | [Jest/Vitest/JUnit/pytest/etc] | [路径] | [范围] |
| 集成测试 | [工具] | [路径] | [范围] |
| E2E 测试 | [Cypress/Playwright/etc] | [路径] | [范围] |

---

## 2. 测试命名

- [命名规范，如：`describe('[模块]', () => { it('should [行为]', ...) })`]
- 文件命名：[模式，如：`*.test.ts`、`*_test.py`]

---

## 3. Mock 规范

### 必须 Mock 的场景

- [外部 API 调用]
- [数据库访问]
- [文件系统操作]
- [定时器/随机数]

### 禁止 Mock 的场景

- [项目内部核心业务逻辑]
- [DTO/VO 纯转换逻辑]

### Mock 工具

- [前端：msw / jest.mock / vitest.mock]
- [后端：Mockito / unittest.mock / pytest-mock]

---

## 4. 覆盖率要求

| 范围 | 目标覆盖率 | 强制执行 |
|------|-----------|----------|
| [范围 1] | [如：80%] | [是/否] |
| [范围 2] | [如：60%] | [是/否] |

---

## 5. 测试数据

- 测试数据管理：[工厂模式 / fixtures / 内联数据]
- 数据库状态：[每个测试前清理 / 事务回滚 / 共享只读数据]
- 敏感信息：[禁止在测试中硬编码真实密钥]

---

## 6. 项目特有测试约定

> 从代码库中提炼，不写通用常识。

- [约定 1：如特定模块必须使用某种测试辅助工具]
- [约定 2：如异步测试必须使用某种模式处理]
- [约定 3：如组件测试必须包含某些断言]

### 正例 / 反例

```[语言]
// 正确
[正确示例代码]

// 错误
[错误示例代码]
```
