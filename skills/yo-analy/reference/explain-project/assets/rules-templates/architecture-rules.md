# 架构规则

> **使用说明**：只保留与该项目相关的架构约定，删除通用常识。按仓库实际类型（前端/后端）删除不适用章节。

---

## 1. 目录与分层约定

### 目录主要结构（如适用）仅供参考

[不需要提供详细的子文件，重点是核心文件和目录]

---

```
[项目根目录]
├── [目录名]/          # [职责]
├── [目录名]/          # [职责]
└── ...
```

- [目录 1]：[职责说明，如 pages/ 放页面级组件]
- [目录 2]：[职责说明，如 components/ 只放纯展示组件]
- [目录 3]：[职责说明，如 hooks/ 放业务无关的通用 Hook]

**禁止**：
- [禁止项 1，如：禁止在 components/ 中直接调用 API]
- [禁止项 2，如：禁止跨 feature 目录直接引用内部模块]

### 后端（如适用）

```
[src/main/java/... 或项目根目录]
├── [层名]/            # [职责]
├── [层名]/            # [职责]
└── ...
```

- [层 1]：[职责，如 controller 只处理 HTTP 请求参数和响应]
- [层 2]：[职责，如 service 处理业务逻辑]
- [层 3]：[职责，如 repository 只负责数据访问]

**禁止**：
- [禁止项 1，如：禁止 controller 直接调用 repository]
- [禁止项 2，如：禁止 service 层处理 HTTP 响应细节]

---

## 2. 模块边界

| 模块 | 路径 | 职责 | 对外暴露 |
|------|------|------|----------|
| [模块 1] | [路径] | [职责] | [公开接口/类型] |
| [模块 2] | [路径] | [职责] | [公开接口/类型] |

**依赖方向**：
```
[模块 A] → [模块 B] → [模块 C]
```

> 禁止反向依赖：[具体说明]。

---

## 3. 数据流约定

### 前端数据流

- [数据流向描述，如：Component → Hook → API Service → Backend]
- 状态管理边界：[全局状态放哪里，局部状态放哪里]
- API 响应处理：[统一错误处理位置、数据转换位置]

### 后端数据流

- [数据流向描述，如：Controller → Service → Repository → DB]
- DTO/Entity 转换：[在哪个层转换]
- 事务边界：[在哪个层开启事务]

---

## 4. 依赖管理

### 允许引入的依赖

- [列表或原则]

### 引入新依赖前必须确认

- [检查项 1，如：是否已有同类库]
- [检查项 2，如：License 是否合规]
- [检查项 3，如：包体积影响]

---

## 5. 项目特有架构约定

> 从代码库中提炼，不写通用常识。

- [约定 1：如特定业务场景必须使用某种设计模式]
- [约定 2：如某种数据必须走缓存层]
- [约定 3：如多模块项目的接口版本管理规则]

### 正例 / 反例

```[语言]
// 正确
[正确示例代码]

// 错误
[错误示例代码]
```
