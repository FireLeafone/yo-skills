# 项目文档

> **使用说明**：按仓库实际类型**删除不适用章节**。标注「仅前端」的章节仅在有 UI/Node 前端工程时填写；标注「仅后端」的章节仅在 Java/Python 等服务端工程时填写；全栈项目可保留两套并分节命名（如「前端技术栈」「后端技术栈」）。

## 1. 项目概述

### 1.1 项目描述
[项目的简要描述，包括项目目标、核心功能等]

### 1.2 技术栈

**前端（仅前端）**

- **前端框架**: [React/Vue/Angular等]
- **状态管理**: [Redux/Vuex/MobX等]
- **UI组件库**: [Ant Design/Element UI/Material-UI等]
- **构建工具**: [Webpack/Vite/等]
- **包管理器**: [npm/yarn/pnpm等]
- **其他关键技术**: [TypeScript/Less/Sass等]

**后端（仅后端）**

- **语言 / 运行时**: [Java 17+ / Python 3.x 等]
- **构建 / 包管理**: [Maven|Gradle|pip|poetry|uv 等]
- **主框架**: [Spring Boot|Django|FastAPI|Flask 等]
- **数据与集成**: [MySQL/PostgreSQL/Redis/消息队列 等，按实际填写]
- **其他**: [安全、任务调度、监控 等]

### 1.3 开发环境要求
- **Node.js版本**（仅前端）: [版本号]
- **JDK / Python**（仅后端）: [版本号]
- **包管理器版本**: [版本号]
- **其他依赖**: [数据库、环境变量等]

### 1.4 环境变量与配置说明
仅列出变量名、所在文件（如 `.env` / `.env.example`）与用途；**不**写入真实密钥、Token、密码。无则写「待补充」。

| 变量名 | 用途 |
|--------|------|
| [例: `VITE_API_BASE`] | [后端 API 基地址] |

## 2. 快速开始

### 2.1 安装依赖
```bash
[安装命令]
```

### 2.2 启动开发服务器 / 本地运行
```bash
[启动命令，如 npm run dev / mvn spring-boot:run / uvicorn ...]
```

### 2.3 构建生产版本
```bash
[构建命令]
```

### 2.4 其他常用命令
```bash
[测试、lint、迁移等命令]
```

## 3. 项目结构

```
[项目目录树结构]
```

### 3.1 核心目录说明
- **[目录名]**: [目录用途说明]

### 3.2 模块划分（仅后端，多模块时）
- **[模块名]**: [职责、依赖关系]

## 4. 开发约定

### 4.1 页面结构约定（仅前端）
[页面组织方式、路由结构、布局模式等]

### 4.2 样式约定（仅前端）
- **样式方案**: [CSS Modules/Styled Components/Less等]
- **命名规范**: [BEM/自定义等]
- **特定样式类**: [全局样式类、工具类等]

### 4.3 交互约定（仅前端）
[常见交互模式、用户体验规范等]

### 4.4 分层与包约定（仅后端）
[Controller/Service/Repository 或 domain 分层、包命名、异常与校验约定等]

### 4.5 配置与环境（仅后端）
[多环境 profile、配置中心、密钥与环境变量约定]

### 4.6 代码规范（通用）
- **命名规范**: [文件命名、变量命名等]
- **代码组织**: [组件/模块结构、文件组织等]
- **注释规范**: [注释风格、文档要求等]

## 5. 通用资源

### 5.1 公共组件（仅前端）
[列出可复用的公共组件及其用途]

- **[组件名]** (`[文件路径]`) - [组件用途] | 示例: [使用示例]

### 5.2 自定义 Hooks（仅前端）
[列出自定义 Hooks 及其用途]

- **[Hook名]** (`[文件路径]`) - [Hook用途] | 示例: [使用示例]

### 5.3 工具函数（前端或通用脚本）
[列出工具函数及其用途]

- **[函数名]** (`[文件路径]`) - [函数用途] | 示例: [使用示例]

### 5.4 常量与枚举（前端或通用）
[列出常量和枚举定义]

- **[常量名]** (`[文件路径]`) - [用途说明] | 值: [值或示例]

### 5.5 TypeScript 类型定义（仅前端）
[列出重要的类型定义]

- **[类型名]** (`[文件路径]`) - [类型用途] | 定义: [类型定义]

### 5.6 可复用后端模块（仅后端）
[公共模块、Starter、SDK 封装、通用异常/响应体等]

- **[模块或类名]** (`[文件路径]`) - [职责] | 示例: [引用或调用方式]

### 5.7 数据访问与集成（仅后端）
[Repository/DAO、Mapper、外部客户端、消息生产消费等]

## 6. 状态管理（仅前端）

### 6.1 状态管理方案
[Redux/Vuex/Context等的使用说明]

### 6.2 全局状态
[全局状态的组织和使用]

## 7. 路由配置（仅前端）

### 7.1 路由结构
[路由配置说明]

### 7.2 路由守卫
[权限控制、路由拦截等]

## 8. API 与对外接口

### 8.1 前端调用约定（仅前端）
[请求封装、baseURL、鉴权、错误处理]

### 8.2 后端对外 API（仅后端）
[REST/gRPC 等风格、版本前缀、OpenAPI/Swagger 位置、认证方式]

### 8.3 接口规范（通用）
[错误码、分页、幂等等团队约定]

## 9. 依赖与集成

### 9.1 测试框架
[前端: Jest/Vitest/Cypress 等 | 后端: JUnit/pytest 等]

### 9.2 测试规范
[单元测试、集成测试的编写规范与目录]

## 10. 部署

### 10.1 构建配置
[生产环境构建配置]

### 10.2 部署流程
[部署步骤和注意事项；容器、镜像、进程管理器如有则写]

## 11. 风险与注意点

基于仓库内可验证信息归纳（README、注释、脚本等），勿编造。

- **已知坑 / 易错点**: [如本地与线上行为差异、必须执行的初始化步骤]
- **环境差异**: [如仅某环境可用的功能、Mock 与真实接口切换]
- **权限与账号**: [如需特定角色、白名单、VPN 等]
- **发布与运维**: [分支策略、发布顺序、回滚注意点等；无则省略]

## 12. 常见问题

[列出开发中的常见问题和解决方案]

## 13. 参考资源与文档

- **内部文档 / Wiki**: [链接或「待补充」]
- **对外或第三方文档**: [链接]
- **建议对接角色**: [产品 / 后端 / 运维等，由读者填写]

[相关文档、教程、工具等链接]
