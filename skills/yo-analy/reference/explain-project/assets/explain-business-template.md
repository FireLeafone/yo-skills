# 业务文档

> **使用说明**：本模板为**业务维度**文档，讲「项目做什么、为什么」，与技术维度文档 `explain-project.md`（讲「怎么建的」）分工不同，勿重复大段内容。
>
> 按**产出端**删除不适用章节：
> - 标注「（仅前端）」的章节仅在产出前端业务文档 `explain-business-frontend.md` 时填写
> - 标注「（仅后端）」的章节仅在产出后端业务文档 `explain-business-backend.md` 时填写
> - 共性章节两端都填；全栈时前端文档的共享章节（业务总结/角色/术语）可引用 `explain-business-backend.md`，只写前端视角补充
>
> 业务语义**基于代码可验证信息归纳，勿编造**；无法确认的写「待补充」或「推测：…」。

## 1. 业务总结

一句话讲清本端为哪类用户解决什么问题、核心价值与业务位置。
- 面向对象：
- 核心价值：
- 在整体业务中的位置：

## 2. 业务角色与参与者

| 角色 | 可执行的业务能力 | 备注 |
|------|------------------|------|
| [角色名] | [能力] | [如调用方系统/真人用户] |

## 3. 主要功能模块

按**业务域 / 页面功能域**组织（非目录）。

### 3.1 [模块名]
- 职责：
- 对应路由/入口（仅前端）：
- 对外能力/关键 Service（仅后端）：
- 主要操作：

## 4. 接口及使用场景

### 4.1 调用的后端接口及使用场景（仅前端）
| 接口 | 触发场景 | 用途 |
|------|----------|------|
| [例: `POST /orders`] | [下单确认页提交] | [创建订单] |

### 4.2 对外接口设计及使用场景（仅后端）
按业务域分组，重点是「为什么存在、谁用」，非逐字段契约。

| 业务域 | 方法+路径 | 用途 | 调用方 | 典型场景 | 幂等 |
|--------|-----------|------|--------|----------|------|
| [域] | [例: `POST /orders`] | [创建订单] | [前端/上游系统] | [用户下单] | [是/否] |

契约细节指向技术文档或 OpenAPI，不在此重复。

## 5. 权限设计及控制

### 5.1 前端权限（仅前端）
- 路由守卫：
- 菜单/按钮级权限：
- 角色与可见功能映射：
- **注意**：前端权限仅为体验层，真实权限以后端为准。

### 5.2 后端权限（仅后端）
- 鉴权方式：[Session/JWT/OAuth/API Key 等]
- 权限模型：[RBAC/ABAC/数据权限]
- 校验落点：[拦截器/注解/中间件/网关]
- 越权防护要点：

## 6. 数据库设计（仅后端）

### 6.1 核心实体关系
```mermaid
erDiagram
%% [核心实体与关系]
```

### 6.2 关键表说明
| 表 | 用途 | 关键关系 | 备注 |
|----|------|----------|------|
| [表名] | [用途] | [主从/外键] | [分库分表/分区] |

### 6.3 迁移与版本管理
[Flyway/Liquibase/Alembic/Django migrations 等]

## 7. 上下游关联服务

### 7.1 上游（谁调用本服务）（仅后端）
| 调用方 | 协议 | 入口 | 量级/SLA |
|--------|------|------|----------|
| [系统] | [HTTP/gRPC/MQ] | [接口] | [若可得知] |

### 7.2 下游（本服务依赖）（仅后端）/ 后端依赖（仅前端）
后端：DB/缓存/MQ/第三方 API/定时任务；前端：依赖的后端 API 基址与网关。

### 7.3 服务拓扑
```mermaid
flowchart
%% [上下游服务拓扑]
```

## 8. 日志设计及控制

### 8.1 前端埋点与上报（仅前端）
- 关键埋点事件、上报通道、日志级别约定。

### 8.2 后端日志（仅后端）
- 日志框架与配置：[级别/格式/输出位置]
- 关键业务日志：[审计日志/操作日志/异常日志 — 在哪打、记什么]
- 链路追踪：[traceId/MDC/OpenTelemetry]

## 9. 领域事件与通知（仅后端）

| 事件/通知 | 触发条件 | 发布通道 | 消费方 |
|-----------|----------|----------|--------|
| [事件名] | [条件] | [MQ topic/webhook] | [消费系统] |

## 10. 业务流程图

```mermaid
flowchart
%% [关键业务流程：前端为页面跳转+操作；后端为跨域链路]
```

## 11. 关键状态机

```mermaid
stateDiagram-v2
%% [核心实体状态流转；前端文档只写前端可见状态，注明后端为权威]
```

## 12. 业务规则与约束

- 业务校验规则：[限额/必填/联动/一致性]
- 异常与补偿：[重试/回滚/对账]
- SLA / 限额：

## 13. 业务核心交互明细

挑选 2–4 个核心链路，逐步描述。
- 前端：触发→请求→响应→状态变化→UI 反馈
- 后端：入口→校验→业务执行→副作用（DB 写/事件/下游调用）→响应

### 13.1 [交互名]
1. ...
2. ...

## 14. 业务术语表

| 术语 | 含义 | 别名 |
|------|------|------|
| [术语] | [含义] | [别名] |

> 全栈时本节以后端文档为引用源，前端文档可链接至此。

## 参考资源与文档

- 内部文档 / Wiki：[链接或「待补充」]
- 关联技术文档：`explain-project.md`（技术维度）
- 建议对接角色：[产品/后端/运维等，由读者填写]
